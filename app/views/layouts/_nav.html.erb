<nav class="navbar navbar-inverse">
   <div class="container">
     <div class="container-fluid">

       <div class="navbar-header">
         <%= link_to root_path, class: "navbar-brand" do %>
           <!<%= image_tag "mumblr-logo-white.png", class: "nav-logo" %>
         <% end %>

         <button type="button" class="navbar-toggle collapsed navbar-right" data-toggle="collapse" data-target="#nav-collapse" aria-expanded="false">
           <span class="sr-only">Toggle navigation</span>
           <span class="icon-bar"></span>
           <span class="icon-bar"></span>
           <span class="icon-bar"></span>
         </button>
       </div>

       <div class="collapse navbar-collapse navbar-right" id="nav-collapse">
         <%= form_tag swim_sessions_path, id: "search", class: "navbar-form", method: :get do %>
           <div class="form-group">
             <%= text_field_tag :search, params[:search], {class: "form-control", placeholder: "Search for swim sessions"} %>
           </div>
           <div class="btn btn-default" onclick="document.getElementById('search').submit()">
             <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
           </div>
         <% end %>

         <% if current_user %>
           <%= link_to('Your profile', user_path(current_user), class: "btn btn-default navbar-btn", type: "button") if params[:controller] != "users" %>
           <%= link_to 'Log out', session_path, class: "btn btn-default navbar-btn", type: "button", method: :delete %>
         <% else %>
           <%= link_to "Log in", new_session_path, class: "btn btn-default navbar-btn", type: "button" %>
           <%= link_to "Register a new account", new_user_path, class: "btn btn-default navbar-btn", type: "button" %>
         <% end %>
       </div> <!-- collapse navbar-collapse navbar-right -->

     </div> <!-- container-fluid -->
   </div> <!-- container -->
 </nav>
